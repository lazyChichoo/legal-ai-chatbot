# 待办事项

最后更新：2026-09-02（第二次）

---

## 🔴 等法学组答复（已问，等回复）

### 1. 知识库第8条 和 第12条 互相矛盾（违约金比例）

| 出处 | 原文 |
|---|---|
| 第8条 ②你该怎么办 | "违约金比例一般不超过预估实际损失的 **20-30%**" |
| 第12条 【风险提示】 | "注意：**20%-30% 是中国司法实践的参考区间，不能直接套用于美国 UCC**" |

- **症状**：用户问"违约金写多少比例合适"时，两条同时被检索到，AI 如实转述两条，
  回答就自相矛盾。**不是 AI 的错，是知识库的错。**
- **需要法学组定**：对美出口场景下到底给哪个说法？第8条那句要不要改？
- **法律上的倾向**：第12条更站得住。美国 UCC §2-718 没有比例上限，
  20-30% 是中国口径（合同法/民法典司法解释）。
- **复现**：`python 篡改测试.py`，看第一遍的回答。
- **状态**：2026-09-02 已问，等回复。

---

## 🟡 待做（技术，我这边能修）

### 2. ✅ 已完成（2026-09-02）检索层改用本地向量库（Chroma）

- **来源**：2026-09-02 用户提出的新要求。
- **现状**：`kb.py` 用的是 TF-IDF 打分，**完全不读 Chroma**；
  队友那套 `ingest.py` / `retrieve.py` 建了 Chroma 库，但问答链路没用上。
- **做法**：向量库出候选（池 12 条）→ 我们的 TF-IDF 精排 → 取 3 条。
  向量库只决定挑哪几条编号，条文正文和 source 仍从 kb_raw.txt 现读。
- **实测**：40 题成绩与改前完全一致；11 题第三顺位换了人，条文覆盖变差 0 题。
- **降级**：没装 chromadb 或没建库 → 自动退回全量扫描，只在 stderr 提示一行。
  ⚠ 降级很安静，演示前务必确认：
  `python -c "import kb;c=kb.chroma_candidates('买方拒收货物怎么办');print('没启用' if c is None else '已启用')"`
- **每台机器要各自建库**（库不进仓库）：`python ingest.py --source kb_raw.txt --reset`
  **kb_raw.txt 改了必须重建**，否则向量库里还是老内容。
- **已知风险（改的时候必须守住）**：
  1. `provisions[...]["source"]` 必须是【法律依据】那一行原文，
     绝不能写成"知识库第9条"，否则 citation_guard 会把 CISG 40/44/50 全判成编造出处。
  2. `kb.PINNED`（拒收→9,13；停运→3,20）必带条文机制不能丢，
     否则 case_guard 强制要求的法条编号会没条文可依，回答被打回吐兜底话术。
  3. `MIN_SCORE` + `MIN_EVIDENCE` 那道"没命中就拒答"的闸门不能丢。
  4. 改完必须重跑 `考卷验收.py`，基线见下方"验收基线"，掉分就是退步。

### 3. ✅ 已完成（2026-09-02）百分数程序级核对（防 AI 编数字）

- **症状**：AI 在【实务提示】里写"建议按合同金额的 **10%-20%**"，
  但喂给它的三条条文里数字只有 `20, 30, 61, 74, 75, 703, 705, 706, 709, 718`，
  **没有 10 —— 是 AI 自己发明的。**
- **根因**：铁律里我自己写了"【实务提示】那一段不受此限"，等于开了个后门。
- **修法**：
  1. 程序卡死 —— 回答里出现的任何百分数，数字必须在喂给 AI 的条文里出现过，
     对不上就打回重答（放 citation_guard，接进 llm.ask 的 problems）。
  2. 铁律 5 收窄 —— 【实务提示】里不许出现任何比例/数字。
- **落地**：`citation_guard.check_numbers()`，接进 `llm.ask` 的 problems。
  实务提示那一段也管（铁律 5 原来给它开的口子已堵）。
  只管百分比，不管法条编号/天数/金额——那些写法太杂，一刀切会误杀。
- **真接口实测**：两遍回答都是「没有编造的比例」，之前那个 10%-20% 不再出现。
- **回归**：`test_e2e.py` 第 8 组 3 项（编造被打回 / 照抄不误杀 / 无百分比不误杀）。
- **顺带修**：`篡改测试.py` 老检查器拿 `"20%" in a` 匹配，被 AI 编的 `10%-20%` 撞上，
  报过一次假绿灯。现在只认区间连写，并自动查有没有编造的比例。
- **顺带修**：`_stub/dotenv.py` 少 `dotenv_values`，导致 chromadb 在测试里导入失败、
  测试一直走降级路径没覆盖向量库。已补齐。


### 4. 铁律 9 逼不出数字 —— 但**先别硬修**，跟法学组那条绑定

- **症状**：条文写明"违约金不超过预估损失的20-30%"，AI 正文里仍然不说，
  只讲"没有固定标准，看是否为合理预估"。写进铁律 9 也没管住，真接口实测两次都是这样。
- **为什么先不硬修**：第12条【风险提示】原文就写着
  "20%-30% 是中国司法实践的参考区间，不能直接套用于美国 UCC"。
  **AI 省略这个数，很可能是它在替我们化解知识库自相矛盾，是对的。**
  这时候用程序逼它说 20-30%，等于逼它给一个第12条明说不适用的建议。
- **所以**：等 #1 法学组裁决完再定。她们说 20-30% 作数 → 再考虑程序强制；
  说不作数 → 该改的是第8条，不是我们的代码。
- **安全性不受影响**：AI 现在编不出数字（#3 已堵死），只是可能少说。少说不危险，编造才危险。

---

## ⚪ 已知但暂不修（记着，别忘了）

4. **铁律 8 没用**：写了"只引用真正用得上的条文"，实测 AI 照样把不相关的条文扯进来。
   目前靠 `MIN_EVIDENCE` 从源头少喂，不靠 AI 自觉。

5. **citation_guard 抓不到"编号对、解释错"**：它只核对法条编号在不在白名单里，
   AI 要是引对编号但把内容说反了，抓不到。属于原理性局限。

6. **《纽约公约》第 III / V 条抓不到**：正则不认罗马数字。
   属于漏检（该抓没抓），不是误杀（不该抓抓了），风险较低。

7. **`DISCLAIMER_EN` 是我自己翻的**，不是法学组的官方英文文本。
   答辩前最好让法学组过一遍。

---

## 验收基线（改任何东西之后都要对一遍，掉了就是退步）

### 检索质量 —— `python 考卷验收.py`（要 `法学组交付/` 目录在旁边）

| | 考卷1.0 短问法 | 考卷2.0 长问法 |
|---|---|---|
| 主场景判对 | 20/20 (100%) | 18/20 (90%) |
| 对应条进了检索结果 | 20/20 (100%) | 20/20 (100%) |
| 条文一条不缺 | 20/20 (100%) | 20/20 (100%) |
| 条文总覆盖率 | 49/49 (100%) | 49/49 (100%) |

### 单元测试（都要"全部通过"，不用 key、不花钱）

```
python test_case_guard.py
python test_scene.py
python test_e2e.py
python test_contract_prescreen.py     ← 45 项
```

### 真接口验证（要 .env 里的 key，只能在用户自己电脑上跑）

```
python 试真接口.py        中/英/知识库外 三道题
python 篡改测试.py        证明知识库真的在驱动回答（改 20-30% → 7%，看回答跟不跟）
python 证明用到知识库.py   打印检索过程和发给 AI 的原文，不调接口、不花钱
```

**`篡改测试.py` 的两行结论现在应该都是"是"**：
```
第一遍回答里出现 20-30% 这个区间：是
第二遍回答里出现 7%              ：是
```

---

## 环境备忘

- 代码在 VM：`/root/legalbot/github_upload/`
- 要发给用户的改动先放 `/root/legalbot/update/`，用户 `scp root@45.129.8.48:/root/legalbot/update/* .`
- ⚠ `scp host:"{a,b,c}" .` **不行** —— 新版 scp 走 SFTP 协议，不做花括号展开。用 `*` 或先归到一个目录。
- ⚠ cmd.exe 会把粘贴的长命令行截断 —— 每条命令都要短。
- 用户仓库目录：`C:\Users\ppeep\Desktop\repo`
- `.env`（DeepSeek key）**只在用户电脑上**，VM 上没有，也不许提交到 GitHub。
